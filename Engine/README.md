# FFmpeg engine

Place a Windows x64 `ffmpeg.exe` build with both `libsoxr` and `libmp3lame`
enabled in this directory before building the application:

```text
Engine/ffmpeg.exe
```

The binary is embedded into the published application but is intentionally not
stored in Git because of its size and separate licensing obligations.
